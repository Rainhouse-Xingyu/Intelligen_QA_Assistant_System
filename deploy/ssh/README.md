# SSH Deploy Key

This directory is intentionally ignored by Git except for this README.

The private key used by the school server must be delivered separately and stored on the server with permission `600`.
Do not commit private keys, `.env`, database dumps with real data, or third-party API credentials.
